import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import React from 'react'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'BeyondRounds - Medical Professional Network',
  description: 'Connect with medical professionals, share knowledge, and advance your career in healthcare.',
  keywords: ['medical', 'healthcare', 'professionals', 'networking', 'doctors', 'nurses'],
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div id="root">
          {children}
        </div>
      </body>
    </html>
  )
}